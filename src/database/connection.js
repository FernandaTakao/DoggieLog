

//connection.js
/*
import * as SQLite from "expo-sqlite";

const useSQLiteContext = async () => {
  const db = await SQLite.openDatabaseAsync("doggielogDB.db");

  return db;
};

export default useSQLiteContext;?*/
